<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Quick Explore</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <!-- Animate.css -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">

    <style>
        :root {
            --primary-color: #4a90e2;
            --secondary-color: #2c3e50;
            --light-gray: #f8f9fa;
            --success-color: #28a745;
            --warning-color: #ffc107;
            --danger-color: #dc3545;
            --form-bg: #ffffff;
            --accent-color: #7952b3;
        }
        
        body {
            background-color: var(--light-gray);
            font-family: 'Poppins', sans-serif;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 2rem 1rem;
            background-image: url('data:image/svg+xml;utf8,<svg width="100" height="100" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg"><path fill="%23f8f9fa" d="M0 0h100v100H0z"/><path d="M0 50 l50 -25 v50z" fill-opacity=".03"/><path d="M100 50 l-50 25 v-50z" fill-opacity=".03"/></svg>');
            background-size: 30px;
        }
        
        .error-container {
            max-width: 800px;
            margin: 0 auto;
            text-align: center;
        }
        
        .error-card {
            background: var(--form-bg);
            border-radius: 20px;
            box-shadow: 0 15px 50px rgba(0,0,0,0.12);
            overflow: hidden;
            border: none;
            position: relative;
            transition: all 0.3s ease;
            transform-style: preserve-3d;
            perspective: 1000px;
        }
        
        .error-card:hover {
            transform: translateY(-5px) rotateX(5deg);
            box-shadow: 0 20px 60px rgba(0,0,0,0.15);
        }
        
        .error-header {
            background: linear-gradient(135deg, var(--accent-color), var(--secondary-color));
            color: white;
            padding: 2rem;
            position: relative;
            overflow: hidden;
        }
        
        .error-header::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 80 80" width="80" height="80"><path fill="none" stroke="white" stroke-width="1" d="M20,20 L80,80 M0,40 L40,0 M60,0 L0,60 M80,20 L20,80" stroke-opacity="0.1"/></svg>');
            background-size: 40px;
            opacity: 0.3;
        }
        
        .error-icon {
            font-size: 5rem;
            margin-bottom: 1rem;
            display: inline-block;
            animation: pulse 2s infinite;
            filter: drop-shadow(0 0 15px rgba(255, 255, 255, 0.3));
        }
        
        @keyframes pulse {
            0% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.05);
            }
            100% {
                transform: scale(1);
            }
        }
        
        .error-title {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
            letter-spacing: -0.5px;
        }
        
        .error-subtitle {
            font-size: 1.2rem;
            opacity: 0.8;
            margin-bottom: 0;
        }
        
        .error-body {
            padding: 2.5rem;
            position: relative;
        }
        
        .error-message {
            background-color: rgba(220, 53, 69, 0.05);
            border-left: 4px solid var(--danger-color);
            border-radius: 10px;
            padding: 1.5rem;
            margin-bottom: 2rem;
            text-align: left;
            animation: fadeIn 1s;
            position: relative;
            z-index: 1;
        }
        
        .error-message-icon {
            position: absolute;
            top: 1.5rem;
            left: 1.5rem;
            font-size: 1.5rem;
            color: var(--danger-color);
        }
        
        .error-message-content {
            padding-left: 40px;
        }
        
        .error-message-title {
            font-weight: 600;
            color: var(--danger-color);
            margin-bottom: 0.5rem;
        }
        
        .error-message-text {
            color: #555;
            font-size: 1rem;
            margin-bottom: 0;
        }
        
        .suggestions {
            margin-bottom: 2rem;
            text-align: left;
        }
        
        .suggestion-title {
            font-weight: 600;
            margin-bottom: 1rem;
            color: var(--secondary-color);
            display: flex;
            align-items: center;
        }
        
        .suggestion-title i {
            margin-right: 0.5rem;
            color: var(--accent-color);
        }
        
        .suggestion-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }
        
        .suggestion-item {
            margin-bottom: 0.8rem;
            padding-left: 25px;
            position: relative;
            animation: fadeInUp 0.5s;
        }
        
        .suggestion-item::before {
            content: "";
            position: absolute;
            left: 0;
            top: 8px;
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background-color: var(--accent-color);
        }
        
        .btn-home {
            background: linear-gradient(to right, var(--primary-color), #2773c3);
            border: none;
            border-radius: 50px;
            padding: 1rem 2.5rem;
            font-weight: 600;
            letter-spacing: 0.5px;
            color: white;
            transition: all 0.3s ease;
            box-shadow: 0 10px 25px rgba(74, 144, 226, 0.3);
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            position: relative;
            overflow: hidden;
            z-index: 1;
        }
        
        .btn-home::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to right, #2773c3, var(--primary-color));
            opacity: 0;
            z-index: -1;
            transition: opacity 0.3s ease;
        }
        
        .btn-home:hover::before {
            opacity: 1;
        }
        
        .btn-home:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 30px rgba(74, 144, 226, 0.4);
            color: white;
        }
        
        .btn-home i {
            margin-right: 10px;
            transition: transform 0.3s ease;
        }
        
        .btn-home:hover i {
            transform: rotate(-360deg);
        }
        
        .background-particles {
            position: absolute;
            width: 200px;
            height: 200px;
            bottom: -100px;
            right: -100px;
            opacity: 0.05;
            transform: rotate(-15deg);
        }
        
        .helper-text {
            font-size: 0.85rem;
            color: #777;
            margin-top: 1.5rem;
        }
        
        .helper-text a {
            color: var(--accent-color);
            text-decoration: none;
            font-weight: 500;
            transition: all 0.2s;
        }
        
        .helper-text a:hover {
            text-decoration: underline;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        
        @keyframes fadeInUp {
            from { 
                opacity: 0;
                transform: translateY(10px);
            }
            to { 
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        /* Animation delay for suggestion items */
        .suggestion-item:nth-child(1) { animation-delay: 0.1s; }
        .suggestion-item:nth-child(2) { animation-delay: 0.2s; }
        .suggestion-item:nth-child(3) { animation-delay: 0.3s; }
        .suggestion-item:nth-child(4) { animation-delay: 0.4s; }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-card animate__animated animate__fadeIn">
            <div class="error-header">
                <i class="fas fa-exclamation-triangle error-icon"></i>
                <h1 class="error-title">Oops! Something went wrong</h1>
                <p class="error-subtitle">We encountered an error processing your request</p>
            </div>
            
            <div class="error-body">
                <div class="error-message">
                    <i class="fas fa-exclamation-circle error-message-icon"></i>
                    <div class="error-message-content">
                        <h4 class="error-message-title">Error Details</h4>
                        <p class="error-message-text">
                            <%= typeof error === 'string' ? error : error && error.message ? error.message : 'An unexpected error occurred. Please try again later.' %>
                        </p>
                    </div>
                </div>
                
                <div class="suggestions">
                    <h4 class="suggestion-title">
                        <i class="fas fa-lightbulb"></i> Here are some suggestions
                    </h4>
                    <ul class="suggestion-list">
                        <li class="suggestion-item">Check your credentials and try again</li>
                        <li class="suggestion-item">Make sure you've entered the correct information</li>
                        <li class="suggestion-item">Try clearing your browser cache and cookies</li>
                        <li class="suggestion-item">If the problem persists, please contact our support team</li>
                    </ul>
                </div>
                
                <a href="/places2" class="btn btn-home animate__animated animate__fadeInUp">
                    <i class="fas fa-home"></i> Back to Home
                </a>
                
                <p class="helper-text">
                    Need help? <a href="#" onclick="window.history.back(); return false;">Go back</a> or contact us at <a href="mailto:support@quickexplore.com">support@quickexplore.com</a>
                </p>
                
                <i class="fas fa-cogs background-particles"></i>
            </div>
        </div>
    </div>
    
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Animation script -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Add pulse animation to error card on hover
            const errorCard = document.querySelector('.error-card');
            
            errorCard.addEventListener('mouseenter', function() {
                document.querySelector('.error-icon').style.animationPlayState = 'paused';
            });
            
            errorCard.addEventListener('mouseleave', function() {
                document.querySelector('.error-icon').style.animationPlayState = 'running';
            });
        });
    </script>
</body>
</html> 