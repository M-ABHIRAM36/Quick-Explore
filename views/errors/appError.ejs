<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Server Error - Quick Explore</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <!-- Animate.css -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">

    <style>
        :root {
            --primary-color: #4a90e2;
            --secondary-color: #2c3e50;
            --accent-color: #e74c3c;
            --light-gray: #f8f9fa;
            --dark-gray: #343a40;
            --form-bg: #ffffff;
        }
        html, body {
            height: 100%; /* Ensure full height */
            overflow-x: hidden; /* Prevent horizontal scrolling */
        }
        body {
            background-color: var(--light-gray);
            font-family: 'Poppins', sans-serif;
            height: 100%; /* Take full height */
            display: flex;
            flex-direction: column;
            justify-content: flex-start; /* Align elements from the top */
            padding: 2rem 1rem;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            position: static; /* Remove position: relative */
            overflow-y: auto; /* Allow vertical scrolling */
        }
        
        body::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('data:image/svg+xml;utf8,<svg width="100" height="100" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg"><path fill="%23ffffff" d="M0 0h100v100H0z"/><path d="M0 50 l50 -25 v50z" fill-opacity=".03"/><path d="M100 50 l-50 25 v-50z" fill-opacity=".03"/></svg>');
            background-size: 30px;
            opacity: 0.6;
            z-index: -1;
        }


        .error-container {
            max-width: 800px;
            margin: 0 auto;
            text-align: center;
            position: relative;
            z-index: 10;
        }
        
        .error-card {
            background: var(--form-bg);
            border-radius: 20px;
            box-shadow: 0 15px 50px rgba(0,0,0,0.12);
            overflow: hidden;
            border: none;
            position: relative;
            transition: all 0.3s ease;
            transform-style: preserve-3d;
            perspective: 1000px;
        }
        
        .error-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 60px rgba(0,0,0,0.15);
        }
        
        .error-header {
            background: linear-gradient(135deg, var(--accent-color), #c0392b);
            color: white;
            padding: 2.5rem 2rem;
            position: relative;
            overflow: hidden;
        }
        
        .error-header::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 80 80" width="80" height="80"><path fill="none" stroke="white" stroke-width="1" d="M20,20 L80,80 M0,40 L40,0 M60,0 L0,60 M80,20 L20,80" stroke-opacity="0.1"/></svg>');
            background-size: 40px;
            opacity: 0.3;
        }
        
        .error-icon {
            font-size: 5rem;
            margin-bottom: 1.5rem;
            display: inline-block;
            animation: shake 1.5s infinite;
        }
        
        @keyframes shake {
            0%, 100% {
                transform: translateX(0);
            }
            20%, 60% {
                transform: translateX(-10px);
            }
            40%, 80% {
                transform: translateX(10px);
            }
        }
        
        .error-title {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
            letter-spacing: -0.5px;
        }
        
        .error-subtitle {
            font-size: 1.2rem;
            opacity: 0.9;
            margin-bottom: 0;
        }
        
        .error-body {
            padding: 2.5rem;
            position: relative;
        }
        
        .error-message {
            background-color: rgba(231, 76, 60, 0.05);
            border-left: 4px solid var(--accent-color);
            border-radius: 10px;
            padding: 1.5rem;
            margin-bottom: 2rem;
            text-align: left;
            animation: fadeIn 1s;
        }
        
        .error-details {
            background-color: var(--dark-gray);
            color: #fff;
            padding: 1.5rem;
            border-radius: 10px;
            text-align: left;
            font-family: monospace;
            max-height: 200px;
            overflow-y: auto;
            margin-bottom: 2rem;
        }
        
        .error-details::-webkit-scrollbar {
            width: 6px;
        }
        
        .error-details::-webkit-scrollbar-track {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 3px;
        }
        
        .error-details::-webkit-scrollbar-thumb {
            background: rgba(255, 255, 255, 0.3);
            border-radius: 3px;
        }
        
        .error-code {
            display: inline-block;
            padding: 0.25rem 0.7rem;
            background-color: var(--accent-color);
            color: white;
            border-radius: 50px;
            font-size: 0.9rem;
            margin-right: 0.5rem;
        }
        
        .error-message-icon {
            position: absolute;
            top: 1.5rem;
            left: 1.5rem;
            font-size: 1.5rem;
            color: var(--accent-color);
        }
        
        .error-message-content {
            padding-left: 40px;
        }
        
        .error-message-title {
            font-weight: 600;
            color: var(--accent-color);
            margin-bottom: 0.5rem;
        }
        
        .error-message-text {
            color: #555;
            font-size: 1rem;
            margin-bottom: 0;
        }
        
        .suggestions {
            margin-bottom: 2rem;
            text-align: left;
        }
        
        .suggestion-title {
            font-weight: 600;
            margin-bottom: 1rem;
            color: var(--secondary-color);
            display: flex;
            align-items: center;
        }
        
        .suggestion-title i {
            margin-right: 0.5rem;
            color: var(--primary-color);
        }
        
        .suggestion-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }
        
        .suggestion-item {
            margin-bottom: 0.8rem;
            padding-left: 25px;
            position: relative;
            animation: fadeInUp 0.5s;
        }
        
        .suggestion-item::before {
            content: "";
            position: absolute;
            left: 0;
            top: 8px;
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background-color: var(--primary-color);
        }
        
        .btn-actions {
            display: flex;
            flex-wrap: wrap;
            gap: 1rem;
            justify-content: center;
        }
        
        .btn-retry, .btn-home {
            background: linear-gradient(to right, var(--primary-color), #2773c3);
            border: none;
            border-radius: 50px;
            padding: 1rem 2rem;
            font-weight: 600;
            letter-spacing: 0.5px;
            color: white;
            transition: all 0.3s ease;
            box-shadow: 0 10px 25px rgba(74, 144, 226, 0.3);
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            position: relative;
            overflow: hidden;
            z-index: 1;
        }
        
        .btn-retry::before, .btn-home::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to right, #2773c3, var(--primary-color));
            opacity: 0;
            z-index: -1;
            transition: opacity 0.3s ease;
        }
        
        .btn-retry:hover::before, .btn-home:hover::before {
            opacity: 1;
        }
        
        .btn-retry:hover, .btn-home:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 30px rgba(74, 144, 226, 0.4);
            color: white;
        }
        
        .btn-retry i, .btn-home i {
            margin-right: 10px;
            transition: transform 0.3s ease;
        }
        
        .btn-retry:hover i {
            animation: spinCycle 1s;
        }
        
        .btn-home:hover i {
            transform: translateX(-5px);
        }
        
        @keyframes spinCycle {
            0% {
                transform: rotate(0deg);
            }
            100% {
                transform: rotate(360deg);
            }
        }
        
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }
        
        /* Responsive adjustments */
        @media (max-width: 767px) {
            .error-icon {
                font-size: 4rem;
            }
            
            .error-title {
                font-size: 2rem;
            }
            
            .error-body {
                padding: 1.5rem;
            }
            
            .btn-actions {
                flex-direction: column;
            }
            
            .btn-retry, .btn-home {
                width: 100%;
            }
        }
        
        .contact-developer {
            margin-top: 2rem;
            padding: 1.5rem;
            background-color: rgba(255, 255, 255, 0.9);
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
            text-align: center;
            max-width: 500px;
            margin-left: auto;
            margin-right: auto;
        }
        
        .contact-developer p {
            color: #555;
            margin-bottom: 0.5rem;
            font-size: 0.95rem;
        }
        
        .response-time {
            font-size: 0.8rem !important;
            color: #777 !important;
            margin-top: 0.5rem;
        }
        
        .contact-link {
            display: inline-flex;
            align-items: center;
            color: var(--primary-color);
            font-weight: 500;
            text-decoration: none;
            padding: 0.5rem 1rem;
            border-radius: 50px;
            background-color: rgba(74, 144, 226, 0.1);
            transition: all 0.3s ease;
        }
        
        .contact-link i {
            margin-right: 0.5rem;
            font-size: 1.1rem;
        }
        
        .contact-link:hover {
            background-color: rgba(74, 144, 226, 0.2);
            transform: translateY(-2px);
            color: var(--primary-color);
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="error-container">
            <div class="error-card animate__animated animate__fadeIn">
                <div class="error-header">
                    <i class="fas fa-server error-icon"></i>
                    <h1 class="error-title">Server Error</h1>
                    <p class="error-subtitle">Something went wrong on our server</p>
                </div>
                
                <div class="error-body">
                    <div class="error-message">
                        <i class="fas fa-exclamation-circle error-message-icon"></i>
                        <div class="error-message-content">
                            <h3 class="error-message-title">Technical Details</h3>
                            <p class="error-message-text">
                                <span class="error-code"><%= typeof statusCode !== 'undefined' ? statusCode : '500' %></span>
                                <%= typeof message !== 'undefined' ? message : 'An unexpected error occurred on our server.' %>
                            </p>
                        </div>
                    </div>
                    
                    <% if (typeof stack !== 'undefined' && process.env.NODE_ENV === 'development') { %>
                    <div class="error-details">
                        <pre><%= stack %></pre>
                    </div>
                    <% } %>
                    
                    <div class="suggestions">
                        <h3 class="suggestion-title">
                            <i class="fas fa-lightbulb"></i>
                            Try the following:
                        </h3>
                        <ul class="suggestion-list">
                            <li class="suggestion-item">Refresh the page and try again</li>
                            <li class="suggestion-item">Clear your browser's cache and cookies</li>
                            <li class="suggestion-item">Check your internet connection</li>
                            <li class="suggestion-item">If the problem persists, please contact our support team</li>
                        </ul>
                    </div>
                    
                    <div class="btn-actions">
                        <a href="javascript:window.location.reload()" class="btn-retry">
                            <i class="fas fa-sync-alt"></i> Retry
                        </a>
                        <a href="/places2" class="btn-home">
                            <i class="fas fa-home"></i> Back to Home
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <div class="contact-developer">
            <p>Need assistance? Contact our support team:</p>
            <a href="mailto:QuickExploreTeam@gmail.com" class="contact-link">
                <i class="fas fa-envelope"></i> QuickExploreTeam@gmail.com
            </a>
            <p class="response-time">We typically respond within 24 hours</p>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Add a custom feature to copy error details
        document.addEventListener('DOMContentLoaded', function() {
            const errorDetails = document.querySelector('.error-details');
            if (errorDetails) {
                errorDetails.addEventListener('dblclick', function() {
                    const range = document.createRange();
                    range.selectNode(errorDetails);
                    window.getSelection().removeAllRanges();
                    window.getSelection().addRange(range);
                    document.execCommand('copy');
                    window.getSelection().removeAllRanges();
                    
                    // Show a small tooltip
                    const tooltip = document.createElement('div');
                    tooltip.style.position = 'absolute';
                    tooltip.style.bottom = '0';
                    tooltip.style.left = '50%';
                    tooltip.style.transform = 'translateX(-50%)';
                    tooltip.style.background = 'rgba(0,0,0,0.7)';
                    tooltip.style.color = '#fff';
                    tooltip.style.padding = '5px 10px';
                    tooltip.style.borderRadius = '3px';
                    tooltip.style.fontSize = '12px';
                    tooltip.innerText = 'Error details copied!';
                    errorDetails.appendChild(tooltip);
                    
                    setTimeout(() => {
                        errorDetails.removeChild(tooltip);
                    }, 2000);
                });
            }
        });
    </script>
</body>
</html>

