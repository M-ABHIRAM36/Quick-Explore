<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Check Rental Status | Quick Explore</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
    <style>
        :root {
            --primary-color: #2563eb;
            --secondary-color: #4f46e5;
            --accent-color: #3b82f6;
            --success-color: #10b981;
            --danger-color: #ef4444;
            --warning-color: #f59e0b;
            --light-color: #f3f4f6;
            --dark-color: #1f2937;
            --border-radius: 12px;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f8fafc;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .page-header {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            padding: 2.5rem 0;
            margin-bottom: 2.5rem;
            border-radius: 0 0 30px 30px;
            position: relative;
            overflow: hidden;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }

        .page-header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url("data:image/svg+xml,%3Csvg width='100' height='100' viewBox='0 0 100 100' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M11 18c3.866 0 7-3.134 7-7s-3.134-7-7-7-7 3.134-7 7 3.134 7 7 7zm48 25c3.866 0 7-3.134 7-7s-3.134-7-7-7-7 3.134-7 7 3.134 7 7 7zm-43-7c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zm63 31c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zM34 90c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zm56-76c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zM12 86c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm28-65c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm23-11c2.76 0 5-2.24 5-5s-2.24-5-5-5-5 2.24-5 5 2.24 5 5 5zm-6 60c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm29 22c2.76 0 5-2.24 5-5s-2.24-5-5-5-5 2.24-5 5 2.24 5 5 5zM32 63c2.76 0 5-2.24 5-5s-2.24-5-5-5-5 2.24-5 5 2.24 5 5 5zm57-13c2.76 0 5-2.24 5-5s-2.24-5-5-5-5 2.24-5 5 2.24 5 5 5zm-9-21c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2zM60 91c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2zM35 41c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2zM12 60c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2z' fill='rgba(255,255,255,0.05)' fill-rule='evenodd'/%3E%3C/svg%3E") repeat;
            opacity: 0.3;
        }

        .btn-home {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            border: none;
            padding: 12px 20px;
            font-size: 16px;
            font-weight: 500;
            border-radius: 10px;
            text-decoration: none;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(79, 70, 229, 0.2);
            position: relative;
            overflow: hidden;
            z-index: 1;
        }

        .btn-home::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 0%;
            height: 100%;
            background: linear-gradient(135deg, var(--secondary-color), var(--primary-color));
            transition: all 0.3s ease;
            z-index: -1;
        }

        .btn-home:hover::before {
            width: 100%;
        }

        .btn-home:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(79, 70, 229, 0.3);
            color: white;
        }

        .btn-home i {
            transition: transform 0.5s ease;
        }

        .btn-home:hover i {
            transform: rotate(360deg);
        }

        .form-card {
            border: none;
            border-radius: var(--border-radius);
            overflow: hidden;
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.08);
            background: white;
            transition: all 0.3s ease;
            position: relative;
        }

        .form-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 5px;
            background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
        }

        .form-card-header {
            padding: 1.5rem;
            border-bottom: 1px solid rgba(0, 0, 0, 0.05);
            background: white;
        }

        .form-card-body {
            padding: 2rem;
        }

        .form-control {
            border-radius: 10px;
            padding: 0.75rem 1rem;
            border: 1px solid #e5e7eb;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .form-control:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
        }

        .form-label {
            font-weight: 500;
            color: #4b5563;
            margin-bottom: 0.5rem;
        }

        .form-icon {
            position: absolute;
            left: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: #9ca3af;
            font-size: 1.1rem;
        }

        .input-with-icon {
            position: relative;
        }

        .input-with-icon .form-control {
            padding-left: 3rem;
        }

        .btn-submit {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            border: none;
            color: white;
            font-weight: 500;
            padding: 0.75rem 1.5rem;
            border-radius: 10px;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(79, 70, 229, 0.2);
        }

        .btn-submit:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(79, 70, 229, 0.3);
        }

        .animate-in {
            animation: fadeIn 0.8s ease-out forwards;
            opacity: 0;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @media (max-width: 768px) {
            .page-header {
                padding: 1.5rem 0;
                margin-bottom: 1.5rem;
            }
        }
        .phoneicon{
            padding-bottom: 1.5rem !important;
        }
    </style>
</head>
<body>
    <!-- Page Header -->
    <div class="page-header text-center">
        <div class="container">
            <h1 class="display-5 fw-bold animate-in">Check Rental Status</h1>
            <p class="lead animate-in" style="animation-delay: 0.1s">Enter your details to view your rental request status</p>
        </div>
    </div>

    <div class="container mb-5">
        <!-- Back Button -->
        <div class="mb-4 animate-in" style="animation-delay: 0.2s">
            <a href="/places2" class="btn-home">
                <i class="fas fa-arrow-left"></i> Back to Home
            </a>
        </div>
        
        <!-- Form Card -->
        <div class="form-card animate-in" style="animation-delay: 0.3s">
            <div class="form-card-header">
                <h4 class="mb-0"><i class="fas fa-search me-2"></i>Enter Your Details</h4>
            </div>
            <div class="form-card-body">
                <form action="/checkRentalStatus" method="POST" id="rentalStatusForm">
                    <div class="mb-4">
                        <label for="objectId" class="form-label">Rental Request ID</label>
                        <div class="input-with-icon">
                            <i class="fas fa-id-card form-icon"></i>
                            <input type="text" class="form-control" id="objectId" name="objectId" placeholder="Enter the 24-character request ID (e.g., 680a52d48b7ffbb03898d0be)" required>
                            <small id="objectIdError" class="form-text text-danger"></small>
                        </div>
                    </div>
                    
                    <div class="mb-4">
                        <label for="contact" class="form-label">Contact Number</label>
                        <div class="input-with-icon">
                            <i class="phoneicon fas fa-phone form-icon"></i>
                            <input type="text" class="form-control" id="contact" name="contact" placeholder="Enter the contact number you provided (10 digits)" required>
                            <small id="contactError" class="form-text text-danger"></small>
                            <small class="form-text text-muted">Format: 10 digits starting with 6-9 (e.g., 9876543210)</small>
                        </div>
                    </div>
                    
                    <div class="mb-4">
                        <label for="email" class="form-label">Email Address</label>
                        <div class="input-with-icon">
                            <i class="fas fa-envelope form-icon"></i>
                            <input type="email" class="form-control" id="email" name="email" placeholder="Enter the email address you provided (Gmail only)" required>
                            <small id="emailError" class="form-text text-danger"></small>
                        </div>
                    </div>
                    
                    <button type="submit" class="btn btn-submit w-100" id="submitBtn" disabled>
                        <i class="fas fa-search-location me-2"></i> Check Status
                    </button>
                </form>
            </div>
        </div>
    </div>

    <!-- Animation Script -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const animatedElements = document.querySelectorAll('.animate-in');
            
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.opacity = '1';
                        entry.target.style.transform = 'translateY(0)';
                        observer.unobserve(entry.target);
                    }
                });
            }, { threshold: 0.1 });
            
            animatedElements.forEach(el => {
                observer.observe(el);
            });

            // Form validation
            const rentalStatusForm = document.getElementById('rentalStatusForm');
            const objectIdInput = document.getElementById('objectId');
            const contactInput = document.getElementById('contact');
            const emailInput = document.getElementById('email');
            const submitBtn = document.getElementById('submitBtn');
            
            // Error elements
            const objectIdError = document.getElementById('objectIdError');
            const contactError = document.getElementById('contactError');
            const emailError = document.getElementById('emailError');
            
            // Validation flags
            let isObjectIdValid = false;
            let isContactValid = false;
            let isEmailValid = false;
            
            // ObjectId validation - MongoDB ObjectID format (24-character hexadecimal string)
            objectIdInput.addEventListener('input', validateObjectId);
            
            function validateObjectId() {
                const objectId = objectIdInput.value.trim();
                const mongoIdRegex = /^[0-9a-fA-F]{24}$/;
                
                if (!objectId) {
                    objectIdError.textContent = 'Request ID is required';
                    isObjectIdValid = false;
                } else if (!mongoIdRegex.test(objectId)) {
                    objectIdError.textContent = 'Please enter a valid Request ID (24 characters)';
                    isObjectIdValid = false;
                } else {
                    objectIdError.textContent = '';
                    isObjectIdValid = true;
                }
                
                toggleSubmitButton();
            }
            
            // Contact validation - Indian phone numbers
            contactInput.addEventListener('input', validateContact);
            
            function validateContact() {
                const contact = contactInput.value.trim();
                const indianPhoneRegex = /^[6-9]\d{9}$/;
                
                if (!contact) {
                    contactError.textContent = 'Contact number is required';
                    isContactValid = false;
                } else if (!indianPhoneRegex.test(contact)) {
                    contactError.textContent = 'Please enter a valid Indian phone number (10 digits starting with 6-9)';
                    isContactValid = false;
                } else {
                    contactError.textContent = '';
                    isContactValid = true;
                }
                
                toggleSubmitButton();
            }
            
            // Email validation - only allow gmail accounts
            emailInput.addEventListener('input', validateEmail);
            
            function validateEmail() {
                const email = emailInput.value.trim();
                const gmailRegex = /^[\w.+-]+@gmail\.com$/;
                
                if (!email) {
                    emailError.textContent = 'Email is required';
                    isEmailValid = false;
                } else if (!gmailRegex.test(email)) {
                    emailError.textContent = 'Only Gmail accounts are allowed';
                    isEmailValid = false;
                } else {
                    emailError.textContent = '';
                    isEmailValid = true;
                }
                
                toggleSubmitButton();
            }
            
            // Toggle submit button based on validation
            function toggleSubmitButton() {
                submitBtn.disabled = !(isObjectIdValid && isContactValid && isEmailValid);
            }
            
            // Validate on form submission
            rentalStatusForm.addEventListener('submit', function(e) {
                validateObjectId();
                validateContact();
                validateEmail();
                
                if (!(isObjectIdValid && isContactValid && isEmailValid)) {
                    e.preventDefault();
                }
            });
        });
    </script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
