<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vehicle Request Status | Quick Explore</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
    <style>
        :root {
            --primary-color: #2563eb;
            --secondary-color: #4f46e5;
            --accent-color: #3b82f6;
            --success-color: #10b981;
            --danger-color: #ef4444;
            --warning-color: #f59e0b;
            --light-color: #f3f4f6;
            --dark-color: #1f2937;
            --border-radius: 12px;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f8fafc;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .page-header {
            background: linear-gradient(135deg, var(--secondary-color), var(--accent-color));
            color: white;
            padding: 2.5rem 0;
            margin-bottom: 2.5rem;
            border-radius: 0 0 30px 30px;
            position: relative;
            overflow: hidden;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }

        .page-header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url("data:image/svg+xml,%3Csvg width='100' height='100' viewBox='0 0 100 100' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M11 18c3.866 0 7-3.134 7-7s-3.134-7-7-7-7 3.134-7 7 3.134 7 7 7zm48 25c3.866 0 7-3.134 7-7s-3.134-7-7-7-7 3.134-7 7 3.134 7 7 7zm-43-7c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zm63 31c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zM34 90c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zm56-76c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zM12 86c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm28-65c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm23-11c2.76 0 5-2.24 5-5s-2.24-5-5-5-5 2.24-5 5 2.24 5 5 5zm-6 60c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm29 22c2.76 0 5-2.24 5-5s-2.24-5-5-5-5 2.24-5 5 2.24 5 5 5zM32 63c2.76 0 5-2.24 5-5s-2.24-5-5-5-5 2.24-5 5 2.24 5 5 5zm57-13c2.76 0 5-2.24 5-5s-2.24-5-5-5-5 2.24-5 5 2.24 5 5 5zm-9-21c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2zM60 91c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2zM35 41c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2zM12 60c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2z' fill='rgba(255,255,255,0.05)' fill-rule='evenodd'/%3E%3C/svg%3E") repeat;
            opacity: 0.3;
        }

        .btn-home {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            border: none;
            padding: 12px 20px;
            font-size: 16px;
            font-weight: 500;
            border-radius: 10px;
            text-decoration: none;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(79, 70, 229, 0.2);
            position: relative;
            overflow: hidden;
            z-index: 1;
        }

        .btn-home::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 0%;
            height: 100%;
            background: linear-gradient(135deg, var(--secondary-color), var(--primary-color));
            transition: all 0.3s ease;
            z-index: -1;
        }

        .btn-home:hover::before {
            width: 100%;
        }

        .btn-home:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(79, 70, 229, 0.3);
            color: white;
        }

        .btn-home i {
            transition: transform 0.5s ease;
        }

        .btn-home:hover i {
            transform: rotate(360deg);
        }

        .vehicle-card {
            border: none;
            border-radius: var(--border-radius);
            overflow: hidden;
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.08);
            background: white;
            transition: all 0.3s ease;
            position: relative;
            width: 600px;
            
        }

        .vehicle-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.12);
        }

        .vehicle-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 5px;
            background: linear-gradient(90deg, var(--secondary-color), var(--accent-color));
        }

        .card-header {
            border-bottom: 1px solid rgba(0, 0, 0, 0.05);
            padding: 1.5rem;
            background: white;
        }

        .card-body {
            padding: 0;
        }

        .vehicle-image-container {
            position: relative;
            height: 300px;
            overflow: hidden;
        }

        .vehicle-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }

        .vehicle-card:hover .vehicle-image {
            transform: scale(1.05);
        }

        .vehicle-details {
            padding: 2rem;
        }

        .detail-item {
            display: flex;
            margin-bottom: 1.2rem;
            align-items: center;
            animation: fadeInUp 0.5s ease forwards;
            opacity: 0;
        }

        .detail-icon {
            width: 40px;
            height: 40px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 1rem;
            font-size: 1.1rem;
            flex-shrink: 0;
            color: white;
        }

        .detail-icon.owner {
            background: linear-gradient(135deg, #4ade80, #10b981);
        }

        .detail-icon.contact {
            background: linear-gradient(135deg, #60a5fa, #3b82f6);
        }

        .detail-icon.email {
            background: linear-gradient(135deg, #f472b6, #ec4899);
        }

        .detail-icon.vehicle {
            background: linear-gradient(135deg, #f97316, #ea580c);
        }

        .detail-icon.brand {
            background: linear-gradient(135deg, #a78bfa, #8b5cf6);
        }

        .detail-icon.model {
            background: linear-gradient(135deg, #facc15, #eab308);
        }

        .detail-icon.registration {
            background: linear-gradient(135deg, #06b6d4, #0891b2);
        }

        .detail-icon.location {
            background: linear-gradient(135deg, #f43f5e, #e11d48);
        }

        .detail-icon.pincode {
            background: linear-gradient(135deg, #14b8a6, #0d9488);
        }

        .detail-icon.status {
            background: linear-gradient(135deg, #6366f1, #4f46e5);
        }

        .detail-icon.date {
            background: linear-gradient(135deg, #8b5cf6, #7c3aed);
        }

        .detail-content {
            flex-grow: 1;
        }

        .detail-label {
            font-size: 0.875rem;
            color: #6b7280;
            margin-bottom: 0.25rem;
        }

        .detail-value {
            font-size: 1.1rem;
            font-weight: 600;
            color: #1f2937;
        }

        .badge {
            font-size: 0.875rem;
            padding: 0.5rem 1rem;
            border-radius: 30px;
            font-weight: 600;
            display: inline-flex;
            align-items: center;
            gap: 6px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .badge.bg-success {
            background: rgba(16, 185, 129, 0.15) !important;
            color: var(--success-color);
        }

        .badge.bg-danger {
            background: rgba(239, 68, 68, 0.15) !important;
            color: var(--danger-color);
        }

        .badge.bg-warning {
            background: rgba(245, 158, 11, 0.15) !important;
            color: var(--warning-color);
        }

        .animate-in {
            animation: fadeIn 0.8s ease-out forwards;
            opacity: 0;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(15px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }

        .pulse {
            animation: pulse 2s infinite;
        }

        .empty-state {
            text-align: center;
            padding: 3rem;
            background-color: white;
            border-radius: var(--border-radius);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        }

        .empty-state i {
            font-size: 4rem;
            color: #e5e7eb;
            margin-bottom: 1.5rem;
        }

        @media (max-width: 768px) {
            .page-header {
                padding: 1.5rem 0;
                margin-bottom: 1.5rem;
            }
            
            .detail-item {
                flex-direction: column;
                align-items: flex-start;
            }
            
            .detail-icon {
                margin-bottom: 0.75rem;
                margin-right: 0;
            }

            .vehicle-image-container {
                height: 200px;
            }
        }
    </style>
</head>
<body>
    <!-- Page Header -->
    <div class="page-header text-center">
        <div class="container">
            <h1 class="display-5 fw-bold animate-in">Vehicle Request Status</h1>
            <p class="lead animate-in" style="animation-delay: 0.1s">Check the current status of your vehicle listing request</p>
        </div>
    </div>

    <div class="container mb-5">
        <!-- Back Button -->
        <div class="mb-4 animate-in" style="animation-delay: 0.2s">
            <a href="/places2" class="btn-home">
                <i class="fas fa-arrow-left"></i> Back to Home
            </a>
        </div>

        <% if (vehicleRequest) { %>
            <!-- Vehicle Card -->
            <div class="vehicle-card animate-in" style="animation-delay: 0.3s">
                <div class="card-header">
                    <h4 class="mb-0"><i class="fas fa-car me-2"></i>Vehicle Request Details</h4>
                </div>
                <div class="card-body">
                    <!-- Vehicle Image -->
                    <div class="vehicle-image-container">
                        <% if (vehicleRequest.images.length > 0) { %>
                            <img src="<%= vehicleRequest.images[0] %>" class="vehicle-image" alt="Vehicle Image">
                        <% } else { %>
                            <img src="https://via.placeholder.com/800x400?text=No+Vehicle+Image" class="vehicle-image" alt="No Image">
                        <% } %>
                    </div>

                    <div class="vehicle-details">
                        <!-- Owner Name -->
                        <div class="detail-item" style="animation-delay: 0.4s">
                            <div class="detail-icon owner">
                                <i class="fas fa-user"></i>
                            </div>
                            <div class="detail-content">
                                <div class="detail-label">Owner Name</div>
                                <div class="detail-value"><%= vehicleRequest.ownerName %></div>
                            </div>
                        </div>
                        
                        <!-- Contact -->
                        <div class="detail-item" style="animation-delay: 0.45s">
                            <div class="detail-icon contact">
                                <i class="fas fa-phone"></i>
                            </div>
                            <div class="detail-content">
                                <div class="detail-label">Contact Number</div>
                                <div class="detail-value"><%= vehicleRequest.contact %></div>
                            </div>
                        </div>
                        
                        <!-- Email -->
                        <div class="detail-item" style="animation-delay: 0.5s">
                            <div class="detail-icon email">
                                <i class="fas fa-envelope"></i>
                            </div>
                            <div class="detail-content">
                                <div class="detail-label">Email Address</div>
                                <div class="detail-value"><%= vehicleRequest.email %></div>
                            </div>
                        </div>
                        
                        <!-- Vehicle Type -->
                        <div class="detail-item" style="animation-delay: 0.55s">
                            <div class="detail-icon vehicle">
                                <i class="fas fa-car-side"></i>
                            </div>
                            <div class="detail-content">
                                <div class="detail-label">Vehicle Type</div>
                                <div class="detail-value"><%= vehicleRequest.vehicleType %></div>
                            </div>
                        </div>
                        
                        <!-- Brand -->
                        <div class="detail-item" style="animation-delay: 0.6s">
                            <div class="detail-icon brand">
                                <i class="fas fa-tag"></i>
                            </div>
                            <div class="detail-content">
                                <div class="detail-label">Brand</div>
                                <div class="detail-value"><%= vehicleRequest.brand %></div>
                            </div>
                        </div>
                        
                        <!-- Model -->
                        <div class="detail-item" style="animation-delay: 0.65s">
                            <div class="detail-icon model">
                                <i class="fas fa-info-circle"></i>
                            </div>
                            <div class="detail-content">
                                <div class="detail-label">Model</div>
                                <div class="detail-value"><%= vehicleRequest.model %></div>
                            </div>
                        </div>
                        
                        <!-- Registration Number -->
                        <div class="detail-item" style="animation-delay: 0.7s">
                            <div class="detail-icon registration">
                                <i class="fas fa-id-card"></i>
                            </div>
                            <div class="detail-content">
                                <div class="detail-label">Registration Number</div>
                                <div class="detail-value"><%= vehicleRequest.registrationNumber %></div>
                            </div>
                        </div>
                        
                        <!-- Location -->
                        <div class="detail-item" style="animation-delay: 0.75s">
                            <div class="detail-icon location">
                                <i class="fas fa-map-marker-alt"></i>
                            </div>
                            <div class="detail-content">
                                <div class="detail-label">Location</div>
                                <div class="detail-value"><%= vehicleRequest.location %></div>
                            </div>
                        </div>
                        
                        <!-- Pincode -->
                        <div class="detail-item" style="animation-delay: 0.8s">
                            <div class="detail-icon pincode">
                                <i class="fas fa-map-pin"></i>
                            </div>
                            <div class="detail-content">
                                <div class="detail-label">Pincode</div>
                                <div class="detail-value"><%= vehicleRequest.pincode %></div>
                            </div>
                        </div>
                        
                        <!-- Status -->
                        <div class="detail-item" style="animation-delay: 0.85s">
                            <div class="detail-icon status">
                                <i class="fas fa-tasks"></i>
                            </div>
                            <div class="detail-content">
                                <div class="detail-label">Status</div>
                                <div class="detail-value">
                                    <span class="badge <%= vehicleRequest.adminStatus === 'Approved' ? 'bg-success' : vehicleRequest.adminStatus === 'Rejected' ? 'bg-danger' : 'bg-warning' %> pulse">
                                        <% if (vehicleRequest.adminStatus === 'Approved') { %>
                                            <i class="fas fa-check-circle"></i>
                                        <% } else if (vehicleRequest.adminStatus === 'Rejected') { %>
                                            <i class="fas fa-times-circle"></i>
                                        <% } else { %>
                                            <i class="fas fa-clock"></i>
                                        <% } %>
                                <%= vehicleRequest.adminStatus %>
                            </span>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Requested On -->
                        <div class="detail-item" style="animation-delay: 0.9s">
                            <div class="detail-icon date">
                                <i class="fas fa-calendar-alt"></i>
                            </div>
                            <div class="detail-content">
                                <div class="detail-label">Requested On</div>
                                <div class="detail-value"><%= new Date(vehicleRequest.createdAt).toDateString() %></div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Status Message -->
            <div class="mt-4 text-center animate-in" style="animation-delay: 0.95s">
                <% if (vehicleRequest.adminStatus === 'Approved') { %>
                    <div class="alert alert-success" role="alert">
                        <i class="fas fa-check-circle me-2"></i> Your vehicle listing request has been approved! Your vehicle will be available for bookings.
                    </div>
                <% } else if (vehicleRequest.adminStatus === 'Rejected') { %>
                    <div class="alert alert-danger" role="alert">
                        <i class="fas fa-exclamation-circle me-2"></i> We're sorry, your vehicle listing request has been rejected.
                    </div>
                <% } else { %>
                    <div class="alert alert-warning" role="alert">
                        <i class="fas fa-hourglass-half me-2"></i> Your request is pending approval. We'll notify you once it's processed.
                    </div>
                <% } %>
            </div>
            
        <% } else { %>
            <!-- Empty State -->
            <div class="empty-state animate-in" style="animation-delay: 0.3s">
                <i class="fas fa-search"></i>
                <h3 class="fw-bold">No Vehicle Request Found</h3>
                <p class="text-muted mb-4">We couldn't find any vehicle request with the provided details.</p>
                <a href="/checkVstatusF" class="btn-home">
                    <i class="fas fa-search me-2"></i> Try Another Search
                </a>
            </div>
        <% } %>
    </div>

    <!-- Animation Script -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const animatedElements = document.querySelectorAll('.animate-in, .detail-item');
            
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.opacity = '1';
                        entry.target.style.transform = 'translateY(0)';
                        observer.unobserve(entry.target);
                    }
                });
            }, { threshold: 0.1 });
            
            animatedElements.forEach(el => {
                observer.observe(el);
            });
        });
    </script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
